package assembler

import "ci.itzana.me/itzaname/sic-xe-asm/assembler/parser/graph"

type Assembler struct {
	graph *graph.Graph
}

func GetBytes(graph *graph.Graph) ([]byte, error) {
	return nil, nil
}
